{
  "name": "saas-app_nextjs-nextjs",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev:web": "pnpm --filter web dev",
    "dev:api": "pnpm --filter api start:dev",
    "dev": "pnpm --filter @repo/database db:check && concurrently \"pnpm dev:web\" \"pnpm dev:api\"",
    "build": "pnpm -r build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "pnpm -r lint",
    "format": "prettier . --write"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.28.0",
  "devDependencies": {
    "@types/node": "^25.6.0",
    "concurrently": "^8.2.2",
    "eslint": "^10.2.0",
    "prettier": "^3.8.3",
    "typescript": "^6.0.3"
  }
}
